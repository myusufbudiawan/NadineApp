// Reports are generated on demand from other domains' data (Section 7: Report
// is not itself a stored entity) — nothing to persist here. Kept as an empty
// module so this domain still follows the mandatory repository/service/
// routes/schema shape (Constitution 0.A #3).
export interface ReportsRepository {}
